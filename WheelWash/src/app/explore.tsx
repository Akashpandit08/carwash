// This screen is unused — WashMate uses index.tsx with internal tab navigation.
// Kept as a no-op to satisfy expo-router's file-based routing.
import React from 'react';
import { View } from 'react-native';

export default function ExploreScreen() {
  return <View />;
}
